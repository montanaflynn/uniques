{
  "name": "uniques",
  "description": "Remove duplicates from an array",
  "version": "0.1.0",
  "main": "./dist/uniques.js",
  "devDependencies": {
    "babel-cli": "^6.2.4",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.2.0",
    "mocha": "^2.2.5"
  },
  "scripts": {
    "build": "babel src --out-dir dist",
    "watch": "babel src --out-dir dist --watch",
    "test": "mocha --ui tdd --compilers js:babel-register",
    "prepublish": "npm run build"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "repository": {
		"type": "git",
		"url": "https://github.com/montanaflynn/uniques.git"
	},
	"keywords": [
		"unique",
		"duplicate",
		"same",
		"type",
		"array"
	],
	"author": "Montana Flynn",
	"license": "MIT"
}
